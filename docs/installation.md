# Installation

Install from PyPI:

```bash
pip install openlifu-sdk
```

For development (editable install with dev extras):

```bash
pip install -e ".[dev]"
```

Build a wheel locally:

```bash
pip install build
python -m build
```